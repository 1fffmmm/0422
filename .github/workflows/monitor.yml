name: Google Drive Monitor

on:
  schedule:
    # 9:15, 15:15, 21:15 (JST)
    - cron: '15 0,6,12 * * *'
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install google-api-python-client google-auth pywebpush supabase

      - name: Run monitor script
        env:
          GCP_SERVICE_ACCOUNT_KEY: ${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}
          DRIVE_FILE_ID: ${{ secrets.DRIVE_FILE_ID }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}  # ここを追加しました
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}  # ここを追加しました
        run: python monitor.py
